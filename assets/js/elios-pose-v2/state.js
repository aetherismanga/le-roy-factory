import{DEFAULT_STATE}from'./constants.js';let current={...DEFAULT_STATE};const listeners=new Set();export const getState=()=>({...current});export function setState(patch){current={...current,...patch};listeners.forEach(fn=>fn(getState()));}export function resetState(){current={...DEFAULT_STATE};listeners.forEach(fn=>fn(getState()));}export function subscribe(fn){listeners.add(fn);return()=>listeners.delete(fn);}