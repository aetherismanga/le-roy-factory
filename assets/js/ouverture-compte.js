const API='https://us-central1-le-roy-factory.cloudfunctions.net/submitAccountRequest';
const PREFILL_API='https://us-central1-le-roy-factory.cloudfunctions.net/getAccountClientPrefill';
const PARTNERS=[
  {name:'Elios Ceramica',logo:'elios.png'},{name:'View Ceramica',logo:'view.png'},{name:'La Fenice',logo:'lafenice.png'},{name:'Reviglass',logo:'reviglass.png'},{name:'Biopietra',logo:'biopietra.png'},{name:"Petracer's",logo:'petracer.png'},{name:'Pecchioli Firenze',logo:'pecchioli.png'},{name:'Bulbo',logo:'bulbo.png'},{name:'Randal Pro',logo:'randal.png'},{name:'Neobath',logo:'neobath.png'},{name:'Koibath',logo:'koibath.png'},{name:'Aquahome',logo:'aquahome.png'},{name:'Opal',logo:'opal.png'},{name:'Bilt',logo:'bilt.png'}
];
const $=s=>document.querySelector(s);
let prefillTimer=null,prefillLoadedKey='',identityLocked=false;

function injectCommercialFields(){
  const adresse=$('#adresse'),cp=$('#codePostal'),ville=$('#ville');if(!adresse||!cp||!ville||$('#modePaiement'))return;
  const label=a=>a.closest('.field')?.querySelector('label');
  if(label(adresse))label(adresse).textContent='Adresse de facturation *';
  if(label(cp))label(cp).textContent='Code postal facturation *';
  if(label(ville))label(ville).textContent='Ville facturation *';
  const grid=adresse.closest('.grid');if(!grid)return;
  const anchor=ville.closest('.field');
  const wrap=document.createElement('div');wrap.className='full';wrap.id='delivery-toggle-wrap';wrap.innerHTML='<label style="display:flex;gap:.55rem;align-items:center;padding:.7rem .8rem;border:1px solid #ddd7cb;border-radius:9px;background:#faf9f6;font-size:.84rem;font-weight:800"><input id="deliverySame" type="checkbox" checked style="width:auto"> Adresse de livraison identique à l’adresse de facturation</label>';
  anchor.insertAdjacentElement('afterend',wrap);
  const d1=document.createElement('div');d1.className='field full delivery-field';d1.innerHTML='<label>Adresse de livraison *</label><input id="adresseLivraison">';
  const d2=document.createElement('div');d2.className='field delivery-field';d2.innerHTML='<label>Code postal livraison *</label><input id="codePostalLivraison" maxlength="5">';
  const d3=document.createElement('div');d3.className='field delivery-field';d3.innerHTML='<label>Ville livraison *</label><input id="villeLivraison">';
  wrap.insertAdjacentElement('afterend',d3);wrap.insertAdjacentElement('afterend',d2);wrap.insertAdjacentElement('afterend',d1);
  const ca=$('#chiffreAffaires')?.closest('.field');
  if(ca){const pay=document.createElement('div');pay.className='field full';pay.innerHTML='<label>Mode de paiement *</label><select id="modePaiement" required><option value="">Choisir…</option><option value="comptant">Comptant</option><option value="mandat_sepa">Mandat SEPA</option></select>';ca.insertAdjacentElement('afterend',pay)}
  $('#deliverySame').addEventListener('change',toggleDeliveryFields);toggleDeliveryFields();
  $('#codePostalLivraison').addEventListener('input',e=>{if(/^\d{5}$/.test(e.target.value)){fetch(`https://geo.api.gouv.fr/communes?codePostal=${e.target.value}&fields=nom&format=json`).then(r=>r.json()).then(d=>{if(d?.[0]?.nom&&!$('#villeLivraison').value)$('#villeLivraison').value=d[0].nom}).catch(()=>{})}});
}
function toggleDeliveryFields(){const same=$('#deliverySame')?.checked!==false;document.querySelectorAll('.delivery-field').forEach(el=>el.style.display=same?'none':'block');['#adresseLivraison','#codePostalLivraison','#villeLivraison'].forEach(sel=>{const el=$(sel);if(el)el.required=!same})}
function renderPartners(selected=[]){const set=new Set((selected||[]).map(String));$('#partners').innerHTML=PARTNERS.map(p=>`<label class="partner"><input type="checkbox" name="partner" value="${p.name.replace(/"/g,'&quot;')}" ${set.has(p.name)?'checked':''}><span class="partner-main"><img class="partner-logo" src="assets/img/${p.logo}" alt="${p.name.replace(/"/g,'&quot;')}" onerror="this.style.display='none'"><span class="partner-name">${p.name}</span></span></label>`).join('')}
function requestType(){return document.querySelector('input[name="requestType"]:checked')?.value||'ouverture'}
function setIdentityLocked(locked){identityLocked=Boolean(locked);['#codeClient','#departementAuth'].forEach(sel=>{const el=$(sel);if(!el)return;el.readOnly=identityLocked;el.setAttribute('aria-readonly',identityLocked?'true':'false');el.style.background=identityLocked?'#f1f3f5':'#fff';el.style.color=identityLocked?'#4b5563':'';el.style.cursor=identityLocked?'not-allowed':''})}
function toggleUpdate(){const update=requestType()==='mise_a_jour';$('#update-auth').style.display=update?'grid':'none';$('#codeClient').required=update;$('#departementAuth').required=update;if(!update){prefillLoadedKey='';setPrefillState('','');setIdentityLocked(false)}else schedulePrefill()}
function depFromCp(cp){const s=String(cp||'').replace(/\s/g,'');if(!/^\d{5}$/.test(s))return'';if(s.startsWith('97')||s.startsWith('98'))return s.slice(0,3);if(s.startsWith('20'))return Number(s)>=20200?'2B':'2A';return s.slice(0,2)}
function fileToPayload(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve({filename:file.name,contentType:file.type||'application/octet-stream',content:String(r.result).split(',')[1]||''});r.onerror=reject;r.readAsDataURL(file)})}
async function collectFiles(){if(!$('#rib').files[0])throw new Error('Le RIB est obligatoire.');if(!$('#kbis').files[0])throw new Error('Le Kbis est obligatoire.');const files=[{role:'rib',file:$('#rib').files[0]},{role:'kbis',file:$('#kbis').files[0]}];[...$('#autresFichiers').files].forEach(file=>files.push({role:'autre',file}));const total=files.reduce((n,x)=>n+x.file.size,0);if(total>12*1024*1024)throw new Error('La taille totale des pièces jointes dépasse 12 Mo.');const out=[];for(const x of files){const p=await fileToPayload(x.file);out.push({...p,role:x.role,size:x.file.size})}return out}
function showMsg(text,ok=false){const el=$('#form-msg');el.textContent=text;el.className=`msg ${ok?'ok':'err'}`}
function normalizeCode(v){let s=String(v||'').trim().toUpperCase().replace(/\s/g,'');if(/^\d{1,5}$/.test(s))s=`LRF-${s.padStart(5,'0')}`;return s}
function setPrefillState(text,type=''){const el=$('#prefill-state');if(!el)return;el.textContent=text;el.className=`full prefill-state ${type}`}
function setSelectValue(select,value){if(!value)return;const exact=[...select.options].find(o=>o.value.toLowerCase()===String(value).toLowerCase());if(exact)select.value=exact.value;else select.value='Autre'}
function fillExisting(c){
  $('#societe').value=c.societe||'';setSelectValue($('#activite'),c.activite||c.categorieActivite||'');
  const ba=c.adresseFacturation||c.adresse||'',bcp=c.codePostalFacturation||c.codePostal||c.code_postal||'',bv=c.villeFacturation||c.ville||'';
  const da=c.adresseLivraison||ba,dcp=c.codePostalLivraison||bcp,dv=c.villeLivraison||bv;
  $('#adresse').value=ba;$('#codePostal').value=bcp;$('#ville').value=bv;$('#adresseLivraison').value=da;$('#codePostalLivraison').value=dcp;$('#villeLivraison').value=dv;
  const same=c.livraisonIdentiqueFacturation!==false&&da===ba&&dcp===bcp&&dv===bv;$('#deliverySame').checked=same;toggleDeliveryFields();
  const mode=String(c.modePaiement||c.paiement||'').toLowerCase().replace(/\s+/g,'_');$('#modePaiement').value=mode.includes('sepa')?'mandat_sepa':mode.includes('comptant')?'comptant':'';
  $('#siret').value=c.siret||'';$('#tva').value=c.tva||'';$('#chiffreAffaires').value=c.chiffreAffaires||c.chiffre_affaires||'';$('#contact').value=c.contact||'';$('#fonction').value=c.fonction||'';$('#telephone').value=c.telephone||(Array.isArray(c.telephones)?c.telephones[0]:'')||'';$('#email').value=c.email||'';$('#emailsAutres').value=Array.isArray(c.emails)?c.emails.join(', '):Array.isArray(c.emailsAutres)?c.emailsAutres.join(', '):'';$('#contactsAutres').value=c.contactsAutres||'';renderPartners(c.partenaires||[]);setIdentityLocked(true)
}
function schedulePrefill(){clearTimeout(prefillTimer);if(requestType()!=='mise_a_jour')return;prefillTimer=setTimeout(loadExisting,250)}
async function loadExisting(){const code=normalizeCode($('#codeClient').value),dep=$('#departementAuth').value.trim().toUpperCase();if(!/^LRF-\d{5}$/.test(code)||!dep){setPrefillState('','');return}const key=`${code}|${dep}`;if(key===prefillLoadedKey)return;setPrefillState('Recherche de votre fiche client…','loading');try{const res=await fetch(PREFILL_API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({codeClient:code,departement:dep})});const d=await res.json().catch(()=>({}));if(!res.ok||!d.success)throw new Error(d.error||'Compte introuvable.');fillExisting(d.client||{});prefillLoadedKey=key;setPrefillState('✓ Votre fiche a été retrouvée et préremplie. Le code client et le département sont verrouillés. Modifiez uniquement les informations à mettre à jour.','ok')}catch(err){prefillLoadedKey='';setPrefillState(err.message||'Impossible de retrouver votre fiche.','err')}}

injectCommercialFields();
const params=new URLSearchParams(location.search);const updateFromLink=params.get('type')==='mise_a_jour';if(updateFromLink)document.querySelector('input[name="requestType"][value="mise_a_jour"]').checked=true;if(updateFromLink){const linkCode=normalizeCode(params.get('code')||''),linkDep=String(params.get('dep')||'').trim().toUpperCase();if(/^LRF-\d{5}$/.test(linkCode)&&linkDep){$('#codeClient').value=linkCode;$('#departementAuth').value=linkDep;setIdentityLocked(true)}}
renderPartners();toggleUpdate();document.querySelectorAll('input[name="requestType"]').forEach(r=>r.addEventListener('change',toggleUpdate));$('#codeClient').addEventListener('input',e=>{if(identityLocked)return;e.target.value=normalizeCode(e.target.value);prefillLoadedKey='';schedulePrefill()});$('#departementAuth').addEventListener('input',()=>{if(identityLocked)return;prefillLoadedKey='';schedulePrefill()});$('#codePostal').addEventListener('input',e=>{if(/^\d{5}$/.test(e.target.value)){fetch(`https://geo.api.gouv.fr/communes?codePostal=${e.target.value}&fields=nom&format=json`).then(r=>r.json()).then(d=>{if(d?.[0]?.nom&&!$('#ville').value)$('#ville').value=d[0].nom}).catch(()=>{})}});

$('#account-form').addEventListener('submit',async e=>{e.preventDefault();const btn=$('#submit-btn');btn.disabled=true;showMsg('Envoi de votre demande en cours…',true);try{const type=requestType(),cp=$('#codePostal').value.trim(),tva=$('#tva').value.trim();if(type==='mise_a_jour'&&!prefillLoadedKey)await loadExisting();if(type==='mise_a_jour'&&!prefillLoadedKey)throw new Error('Votre code client et votre département doivent être reconnus avant l’envoi.');const same=$('#deliverySame').checked;const adresseLivraison=same?$('#adresse').value.trim():$('#adresseLivraison').value.trim(),codePostalLivraison=same?cp:$('#codePostalLivraison').value.trim(),villeLivraison=same?$('#ville').value.trim():$('#villeLivraison').value.trim(),modePaiement=$('#modePaiement').value;if(!modePaiement)throw new Error('Choisissez le mode de paiement : Comptant ou Mandat SEPA.');if(!same&&(!adresseLivraison||!codePostalLivraison||!villeLivraison))throw new Error('Renseignez l’adresse de livraison complète.');const payload={requestType:type,codeClient:type==='mise_a_jour'?normalizeCode($('#codeClient').value):'',departementAuth:type==='mise_a_jour'?$('#departementAuth').value.trim().toUpperCase():'',societe:$('#societe').value.trim(),activite:$('#activite').value.trim(),adresse:$('#adresse').value.trim(),codePostal:cp,ville:$('#ville').value.trim(),departement:depFromCp(cp),adresseFacturation:$('#adresse').value.trim(),codePostalFacturation:cp,villeFacturation:$('#ville').value.trim(),adresseLivraison,codePostalLivraison,villeLivraison,livraisonIdentiqueFacturation:same,modePaiement,siret:$('#siret').value.trim(),tva,chiffreAffaires:$('#chiffreAffaires').value.trim(),contact:$('#contact').value.trim(),fonction:$('#fonction').value.trim(),telephone:$('#telephone').value.trim(),email:$('#email').value.trim(),emailsAutres:$('#emailsAutres').value.split(',').map(x=>x.trim()).filter(Boolean),contactsAutres:$('#contactsAutres').value.trim(),partenaires:[...document.querySelectorAll('input[name="partner"]:checked')].map(x=>x.value),demande:$('#demande').value.trim(),consent:true,attachments:await collectFiles(),source:'formulaire_public',submittedAt:new Date().toISOString()};if(type==='mise_a_jour'&&!/^LRF-\d{5}$/.test(payload.codeClient))throw new Error('Le code client doit être au format LRF-00000.');const res=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),data=await res.json().catch(()=>({}));if(!res.ok||!data.success)throw new Error(data.error||'Impossible d’envoyer la demande.');$('#account-form').reset();prefillLoadedKey='';setIdentityLocked(false);toggleUpdate();toggleDeliveryFields();renderPartners();showMsg(`Votre demande a bien été transmise à LE ROY FACTORY. Référence : ${data.reference||data.id||''}`,true);window.scrollTo({top:0,behavior:'smooth'})}catch(err){console.error(err);showMsg(err.message||'Une erreur est survenue.')}finally{btn.disabled=false}});